name: Hosting paused (firebase-hosting-pull-request)
on: workflow_dispatch
permissions:
  contents: read
jobs:
  hosting-paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hosting deployment is paused during enterprise migration. Review docs/enterprise/MIGRATION.md."

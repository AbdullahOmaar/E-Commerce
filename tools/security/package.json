{
  "name": "e-commerce-security-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.22.3" },
  "scripts": {
    "test": "firebase emulators:exec --project demo-ecommerce --config ../../firebase.security.json --only firestore,storage 'node --test rules.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.0.0",
    "firebase-tools": "15.30.2"
  }
}
